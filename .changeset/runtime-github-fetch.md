---
"@smicolon/ai-kit": minor
---

Runtime GitHub fetching: packs are downloaded from GitHub at runtime instead of being bundled in the npm package. Adds standalone binary builds (brew, curl install script), cache management, and --no-cache/--branch flags.
