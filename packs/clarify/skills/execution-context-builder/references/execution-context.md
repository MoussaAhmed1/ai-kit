# Execution Context: {{task.title}}

> Generated by `/clarify` on {{metadata.created_at}}.
> Source: {{task.source | default: "free-text"}}
> Slug: `{{task.slug}}`

## Entity

{{entity}}

## Trigger

- **Event:** {{trigger.event}}
- **Timing:** {{trigger.timing}}
- **Preconditions:**
{{#each trigger.preconditions}}
  - {{this}}
{{/each}}

## Action

- **Verb:** {{action.verb}}
- **Channel:** {{action.channel}}
- **Recipient:** {{action.recipient}}

## Flow

- **Services involved:** {{#each flow.services}}`{{this}}`{{#unless @last}}, {{/unless}}{{/each}}
- **Mode:** {{flow.mode}}
- **Transport:** {{flow.transport | default: "n/a"}}

## Failure Handling

- **Strategy:** {{failure_handling.strategy}}
- **Details:** {{failure_handling.details}}

## Out of Scope

{{#each out_of_scope}}
- {{this}}
{{/each}}

---

## Metadata

- **Created at:** {{metadata.created_at}}
- **Created by:** {{metadata.created_by}}

## Notes

This artifact is the implementation contract for the task above. If during implementation you discover that the chosen flow is wrong, **stop and re-clarify** rather than silently changing scope. Delete this file and re-run `/clarify`, or pick a different candidate manually.

Downstream packs may consume this artifact:
- `dev-loop` — pre-populates the TDD plan's Goal and Out of scope sections.
- `failure-log` — tracks resolved assumptions.
